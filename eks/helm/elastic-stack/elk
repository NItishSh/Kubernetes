helm repo add elastic https://helm.elastic.co
helm upgrade --install --force elasticsearch elastic/elasticsearch --namespace mgmt
helm upgrade --install --force kibana elastic/kibana --namespace mgmt
